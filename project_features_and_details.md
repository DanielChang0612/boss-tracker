# ⚡ PiKaPi 經驗值小助手 - 完整功能與技術實作細節手冊

> **專案名稱**：PiKaPi 經驗值小助手 (PiKaPi EXP Helper)  
> **軟體作者**：豪小子  
> **軟體版本**：v1.0.0  
> **核心設計**：可愛經典楓之谷風格 × 現代 Liquid Glass 液態毛玻璃半透明懸浮窗  
> **支援作業系統**：macOS (Apple Silicon M1/M2/M3/M4) 及 Windows 10/11 (64-bit)

---

## 📑 目錄
1. [系統架構與技術棧](#1-系統架構與技術棧)
2. [雙介面檢視模式](#2-雙介面檢視模式)
3. [核心演算法與技術細節](#3-核心演算法與技術細節)
4. [記錄與計時狀態機機制](#4-記錄與計時狀態機機制)
5. [快捷鍵與操作指南](#5-快捷鍵與操作指南)
6. [安裝檔與執行檔路徑](#6-安裝檔與執行檔路徑)
7. [開發與編譯指令](#7-開發與編譯指令)

---

## 1. 系統架構與技術棧

```
┌────────────────────────────────────────────────────────┐
│               PiKaPi 經驗值小助手 (Desktop App)          │
├───────────────────────────┬────────────────────────────┤
│   Electron 44.2 (主行程)   │   React 19 + Vite 8 (渲染) │
│  - 螢幕擷取 (screencapture)│  - Liquid Glass UI 介面    │
│  - 全域快捷鍵 (F6 ~ F11)  │  - Tesseract.js OCR 引擎   │
│  - 視窗縮放與穿透控制     │  - 經驗與升等演算法計算器  │
└───────────────────────────┴────────────────────────────┘
```

| 模組 | 技術 / 工具 | 說明 |
| :--- | :--- | :--- |
| **桌面端核心** | Electron 44.2.0 | Chromium 134 + Node.js 22.14 跨平台運行環境 |
| **前端框架** | React 19.2.8 | 高效能組件化狀態管理與即時重繪 |
| **建置工具** | Vite 8.2.2 | 極速 HMR 熱模組替換與最佳化生產打包 |
| **文字辨識** | Tesseract.js 7.0.0 | WebAssembly + Web Worker 多執行緒本機離線 OCR 引擎 |
| **螢幕擷取** | 原生呼叫 / DesktopCapturer | macOS 透過 `screencapture -x`（純淨無游標）；Windows 透過 `desktopCapturer` + 多螢幕 DPI 換算 |
| **樣式美學** | Vanilla CSS | CSS Custom Properties、動態毛玻璃 (`backdrop-filter`)、點陣銳化 (`pixelated`) |

---

## 2. 雙介面檢視模式

### 🖥️ 模式 A：儀表板模式 (Dashboard Mode，預設視窗)
- **左側數據面板**：
  - `M 記憶體`：顯示目前系統工作記憶體用量。
  - `T 統計時間`：精準記錄實際打怪累積時間（`HH:MM:SS`）。
  - `E EXP:`：即時顯示目前經驗值與百分比，例如 `53,658 [7.56%]`。
  - `Lv 等級`：自動依據當前經驗與百分比精準反推等級；支援點選 `[自動 ✎]` 切換為手動指定等級（1~200 等）。
  - `A 累積獲得`：打怪期間累積增加的總經驗值量（例如 `+48,200`）。
  - `% 累積獲得%`：換算為當前等級的百分比進度增量。
  - `S EXP / 分`：依據即時刷怪速率每秒動態計算每分鐘獲得經驗。
  - `10 預估 10 分`：即時推估維持當前節奏 10 分鐘後的累積經驗獲得量。
  - `60 預估 60 分`：即時推估 1 小時後獲得的總經驗值。
  - `剩 升等還差`：距離升到下一級尚需打多少 EXP。
  - `U 升級預估`：即時動態 ETA 倒數時間（`HH:MM`），每秒隨效率浮動。
- **右側監控與微調區**：
  - **實時截圖預覽（Live Preview）**：每秒更新最新擷取的 EXP 畫面，附有綠色 `LIVE 實時更新` 指示標籤。
  - **清晰度倍率切換**：提供 **`1.5x` / `2.0x` / `2.5x`** 快速放大按鈕，預設以 **2.0x 大圖** 呈現，配合點陣銳化邊緣無虛化。
  - **十字座標微調按鍵（D-Pad）**：長按即可連續步進微調 X、Y 座標與選框寬度（W）、高度（H）。
  - **OCR 原始日誌（Log）**：即時印出每秒辨識出的原始字串、置信度與解析結果。

---

### 🎮 模式 B：遊戲精簡模式 (Compact Mode，快捷鍵 F9)
- **極致輕量半透明懸浮窗**：專為遊戲練功設計，尺寸 `300 x 390`，不遮擋主要遊戲視野。
- **頂部狀態欄與呼吸燈**：
  - 綠色動態呼吸光點 **「● 即時追蹤」**，一眼即知 OCR 背景掃描是否正常運行。
  - **單鍵切回主視窗（`⛶`）**：滑鼠點擊即可立即返回儀表板主視窗。
- **快捷操作膠囊鈕**：
  - `▶ 開始記錄 (F11)`：未記錄時呈現明亮綠色發光；記錄中轉為紅色 `⏹ 停止記錄 (F11)`。
  - `🖱 穿透 (F10)`：一鍵切換滑鼠穿透模式（開啟時呈現亮橘色發光）。
- **可自訂外觀抽屜（展開設定）**：
  - 數值字體大小調整（13px ~ 26px）。
  - 數值文字顏色切換（白色、天藍、嫩綠、檸黃、亮橘、桃粉等 6 色調色盤）。
  - 標籤文字大小與顏色調整。
  - 視窗黑白亮度（0 ~ 255）與毛玻璃半透明度（0% ~ 100%）。

---

## 3. 核心演算法與技術細節

### ① 智慧型文字行隔離演算法 (Smart Text-Row Isolation)
- **問題**：楓之谷經驗框上方為文字、下方為黃/灰色進度條。Tesseract 在單行模式（PSM 7）遇到雙層圖案會被進度條干擾而產生亂碼或辨識失敗。
- **實作細節**：
  1. **色彩篩選器**：在 Canvas 原生點陣資料中逐行掃描字元特徵：
     - 白色文字：明度 $Y = 0.299R + 0.587G + 0.114B > 130$
     - 萊姆綠括號與百分比：$G > 130$ 且 $G > 1.05R$ 且 $G > 1.2B$
  2. **自動定位文字邊界**：定位出文字開始行 `textStartY` 與結束行 `textEndY`（利用文字下方 1~3px 的黑邊空隙隔開進度條）。
  3. **100% 抹除進度條**：文字邊界以外的區域（包含下方進度條本體與邊框）全部塗成純白底色，**徹底根除進度條對 OCR 的干擾**。
  4. **4.0 倍像素銳化與邊距填充**：提供 Tesseract 乾淨的單行黑白高對比點陣影像。

---

### ② 楓之谷經驗表反向適配演算法 (Level Fit Optimizer)
- **問題**：楓之谷像素字型小數點只有 1px 容易漏讀（例如 `7.56%` 被讀成 `75.6%`），`%` 斜線容易被誤認為數字 `1`（造成 `75.61%`），導致推算總經驗縮小 10 倍而誤判等級為 48 等。
- **實作細節**：
  1. 內建 1~200 等官方真實升級經驗表（`exp_table.json`）。
  2. 自動生成所有候選排列（例如 `7.56`、`75.6`、`75.61`）。
  3. 依據公式計算推估升級總量：
     $$\text{推算總需經驗} = \frac{\text{當前經驗}}{\text{候選百分比} / 100}$$
  4. 比對經驗表：
     - 若為 $75.61\%$：推算總量 70,967，無高階等級精準匹配。
     - 若為 $7.56\%$：$53658 / 0.0756 = 709,761.9$。查表 50 等升級需求為 $709,716$，相對誤差僅 **0.006%**！
  5. 系統自動選定百分比為 **7.56%**，並精確鎖定等級為 **50 等**！
  6. 若截圖未抓到百分比，自動依據等級總量計算出精準百分比，畫面上永不缺漏。

---

### ③ 實時動態速率引擎 (Real-time Pace Engine)
- **問題**：固定大週期或平滑歷史平均會造成效率數據反應遲鈍或定格。
- **實作細節**：
  - 系統精確記錄首隻怪擊殺的毫秒級時間戳記 `startTime`，每秒依據經過秒數即時計算：
    $$\text{實時分均經驗} = \frac{\Delta \text{獲得 EXP}}{(\text{當前時間} - \text{啟動時間})\text{秒} / 60}$$
    $$\text{預估 10 分鐘獲得} = \text{實時分均經驗} \times 10$$
    $$\text{預估 60 分鐘獲得} = \text{實時分均經驗} \times 60$$
    $$\text{升級預估時間 (ETA)} = \frac{\text{升等還差 EXP}}{\text{實時分均經驗}}$$
  - 數值每秒動態平滑跳動，真實反映開加倍、刷怪速度快慢與換圖休息節奏。

---

### ④ 連續性自我校正與防呆過濾
- **單調遞增檢查**：同一等級內經驗值不應倒退，單影格誤讀低數值時自動過濾保護。
- **連續 3 秒確認**：若數值連續 3 秒偏低（例如死亡掉趴、換分身或重新選區），系統自動校正接受新數值，防止數值永久卡死。
- **跳躍保護上限放寬**：將單次異常跳動保護閥值提升至 150,000，確保高等級獲得巨量經驗或任務結算時不被誤當雜訊攔截。

---

## 4. 記錄與計時狀態機機制

```
[ 選取 EXP 區域 ]
       ↓
[ 點擊 ▶ 開始記錄 (F11) ]
       ↓
鎖定當前 EXP 為基數，進入「⏳ 等待經驗值浮動中...」
(此時統計時間保持 00:00:00，不偷跑計時！)
       ↓
[ 打死第一隻怪物，EXP 增加！ ]
       ↓
⚡ 瞬間觸發：計時器立即從 00:00:01 開始啟動！
累積獲得 EXP、累積獲得 %、每分鐘/小時預估即刻跳動！
       ↓
[ 再次點擊 ⏹ 停止記錄 (F11) ]
       ↓
計時凍結，最終成果數據完整保留於畫面上以供檢視！
```

---

## 5. 快捷鍵與操作指南

| 快捷鍵 | 功能名稱 | 作用說明 |
| :---: | :--- | :--- |
| **`F6`** | **重設所有設定** | 將外觀設定（字體大小/顏色/透明度）與主視窗縮放比例重設為預設值 |
| **`F7`** | **暫停 / 繼續統計** | 暫停當前 OCR 辨識與計時累計，再次按下即可繼續 |
| **`F8`** | **重置數據** | 清空累積獲得經驗與計時器，以當前 EXP 為新基準重啟等待浮動 |
| **`F9`** | **切換模式** | 在「儀表板主視窗」與「遊戲精簡懸浮窗」之間自由切換 |
| **`F10`** | **切換滑鼠穿透** | 切換滑鼠點擊穿透模式（啟用時按鈕發亮，滑鼠點擊直穿遊戲） |
| **`F11`** | **開始 / 停止記錄** | 啟動等待經驗浮動計時，或停止並凍結統計結果供檢視 |

---

## 6. 安裝檔與執行檔路徑

所有程式碼重構與功能已編譯打包至 `dist-electron/` 目錄：

- **macOS（Apple Silicon / M1 / M2 / M3 / M4 系列）**：
  - 📀 [PiKaPi 經驗值小助手-1.0.0-arm64.dmg](file:///Users/Daniel.S_Chang/Documents/實用小工具/測試經驗小工具/dist-electron/PiKaPi%20經驗值小助手-1.0.0-arm64.dmg)（DMG 安裝映像檔）
  - 🗜️ [PiKaPi 經驗值小助手-1.0.0-arm64-mac.zip](file:///Users/Daniel.S_Chang/Documents/實用小工具/測試經驗小工具/dist-electron/PiKaPi%20經驗值小助手-1.0.0-arm64-mac.zip)（免安裝隨身壓縮包）
- **Windows（64 位元）**：
  - 💿 [PiKaPi 經驗值小助手 Setup 1.0.0.exe](file:///Users/Daniel.S_Chang/Documents/實用小工具/測試經驗小工具/dist-electron/PiKaPi%20經驗值小助手%20Setup%201.0.0.exe)（自動安裝版）
  - 📦 [PiKaPi 經驗值小助手 1.0.0.exe](file:///Users/Daniel.S_Chang/Documents/實用小工具/測試經驗小工具/dist-electron/PiKaPi%20經驗值小助手%201.0.0.exe)（免安裝單一執行檔綠色版）

---

## 7. 開發與編譯指令

```bash
# 安裝依賴套件
npm install

# 本機開發模式 (Vite + Electron 同時啟動)
npm run dev:electron

# 建置網頁端靜態資源
npm run build

# 執行程式碼品質與語法檢查
npm run lint

# 打包 macOS 安裝檔
npm run package:mac

# 打包 Windows 安裝檔
npm run package:win

# 同時打包雙平台安裝檔
npm run package:all
```
